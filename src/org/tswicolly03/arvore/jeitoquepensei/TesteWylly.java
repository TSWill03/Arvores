package org.tswicolly03.arvore.jeitoquepensei;

public class TesteWylly {
    public int addNo(No no, int valor) {
        if (no.getValor() == valor) {
            return 0;
        }
        return -1;
    }
}
